import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ["wqbaxhukllenhsbovhhr.supabase.co"],
  },
};

export default nextConfig;
